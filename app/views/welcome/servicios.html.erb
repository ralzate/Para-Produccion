
            <% @todos_los_servicios.each do |servicio| %>

            <div class="col-sm-3">
              <div class="col-item">


                <div class="info">
                  <div class="row">
                    <div class="price col-md-12">
                      <h5 class="title">
                        <%= servicio.tipo %><br/>
                        <%= servicio.descripcion.html_safe %><br/>
                        <%= image_tag servicio.imagen.url, :size =>"40x40" %>

                      <h5>

                    </div>

                  </div>
                </div>
              </div>
            </div>
            <% end %>